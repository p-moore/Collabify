from django.shortcuts import render
#import stuff from board.models.py to get methods
#gonna need to get cards, users, and teams probably
# Create your views here.

#google how to check if a user is logged in
'''
@login_required #import everything required to do this
def board(request):
    return render(request, 'board/board.html')
'''
def board(request):
	pass
'''
def home(request):
	context = {
		Users.objects.all() #this would import all users from database
	}
'''